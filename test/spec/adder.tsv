# adder.tsv — the "adder" directive (open="add<", close=">") over the
# mini test grammar. Action: sums the numbers in the list body; an empty
# body yields 0. add<[1, 2]> → 3.
# Format: <input>\t<expected-json>    or    <input>\t!error <regex>

add<[1, 2]>	3
add<[1, 2, 3]>	6
add<[10]>	10
add<[]>	0

# --- nested in the mini grammar's containers ---
[add<[1, 2]>, add<[3, 4]>]	[3, 7]
{x:add<[1, 2]>}	{"x": 3}
